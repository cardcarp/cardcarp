# Root that storage.js locations resolve against. Use / to serve files from public/.
VITE_STORAGE_ROOT=https://files.example.com

# Relay origin only, no path. Leave unset to use the local relay on ws://localhost:8787.
VITE_MULTIPLAYER_WS_URL=https://relay.example.com
